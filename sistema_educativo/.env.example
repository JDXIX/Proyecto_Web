# Django Configuration
SECRET_KEY=change_me_to_a_random_secret_key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS Configuration (adjust the frontend port as needed)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Database Configuration (SQLite is used by default)
# For production, uncomment and configure DATABASE_URL:
# DATABASE_URL=postgres://user:password@localhost:5432/database_name

# AI Recommendations API Key (get from https://console.anthropic.com/)
# This is required for the personalized recommendations feature
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Alternative: CLAUDE_API_KEY works as well
# CLAUDE_API_KEY=your_claude_api_key_here

# JWT Token Configuration (optional - defaults are set in settings.py)
# ACCESS_TOKEN_LIFETIME_MINUTES=10
# REFRESH_TOKEN_LIFETIME_DAYS=1